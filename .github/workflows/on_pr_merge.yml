name: 🔵 Deploy to Staging
on:
    pull_request:
        branches:
        - main
        types: [closed]
jobs:
    confirm-merge:
        if: ${{ github.event.pull_request.merged }}
        runs-on: ubuntu-latest
        steps:
            - name: 'Confirm'
              run: echo "Just checking pr is merge and not closed"