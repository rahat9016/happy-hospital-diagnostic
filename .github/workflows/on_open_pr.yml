name: 🧪 Perform Tests
on:
  pull_request:
    branches:
      - main

jobs:
  build-and-upload:
    runs-on: ubuntu-latest
    steps:
      - name: "Build & Deploy"
        run: echo "I only run on PRs targeting main"
